import inputText from './Input'
// web view側でログインするためのコンポーネント

export default loginPage () => {
    // Input.tsxで入力したIDとパスワードの取得

    // 入力されt情報をログイン用のwaseda pageに投げる post によるhttp通信でいいはず。

    // セッションキー取得

    // セッションキーをweb viewに渡してログインする。
}
